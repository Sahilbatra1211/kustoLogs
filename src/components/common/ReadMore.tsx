import * as React from 'react';
import {useState} from 'react';

export default function ReadMore(props) {
    
    const [completeText, setCompleteText] = useState(props.text);
    
    return (
        <div>
            
        </div>
    )
}
